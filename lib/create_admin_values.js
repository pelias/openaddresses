/**
 * @file Inferences admin values for an OpenAddresses CSV file given its
 * basename.
 */

'use strict';

function createAdminValues( basename ){
  return {
    country: 'us'
  };
}

module.exports = basename;
